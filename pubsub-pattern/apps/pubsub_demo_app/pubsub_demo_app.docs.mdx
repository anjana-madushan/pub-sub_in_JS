---
description: 'A pubsub_demo_app app.'
labels: ['app', 'react']
---

A React application for pubsub_demo_app. 

## Run the application

Import the app and add the following config to your workspace.jsonc:

```
bit use pubsub_demo_app 
```

To run the app locally, run the following:

```
bit run pubsub_demo_app 
```
